const showMore= documnent.querySelectorAll(".show-more");
const option = documnent.querySelectorAll(".option");
showMore.addEventlistener("click",(e) => {
  option.classList.toggle(console.log("hello"))
});